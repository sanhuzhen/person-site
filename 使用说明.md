# 🎯 Preason Vite 使用说明

## 📁 配置文件

所有设置都在 **`config.json`** 文件中，直接修改即可：

```json
{
  "profile": {
    "avatar": "",           // 头像URL，留空显示默认占位符
    "nickname": "你的名字",  // 昵称
    "description": "描述"   // 个人描述
  },
  "lists": {
    "skills": [],          // 技能列表
    "projects": [],        // 项目列表
    "interests": []        // 兴趣列表
  },
  "theme": {
    "primaryColor": "#667eea",  // 主色调
    "backgroundColor": "#ffffff", // 背景色（白色）
    "textColor": "#333333"      // 文字颜色
  }
}
```

## 🚀 快速开始

```bash
# 1. 修改配置
# 打开 config.json 文件，修改为你自己的内容

# 2. 启动项目
cd D:\vsProjects\preason-vite
npm run dev

# 3. 访问页面
# 浏览器打开 http://localhost:5173
```

## 🎨 常见修改

### 修改头像
```json
"avatar": "https://avatars.githubusercontent.com/u/你的GitHubID"
```
或留空 `""` 使用默认占位符

### 修改背景色
```json
"backgroundColor": "#ffffff"  // 白色
"backgroundColor": "#f8f9fa"  // 浅灰
```

### 添加列表项
```json
"skills": [
  { "id": "1", "title": "React", "content": "前端框架", "icon": "⚛️" },
  { "id": "2", "title": "你的技能", "content": "描述", "icon": "✨" }
]
```

## 📝 修改步骤

1. **打开文件**: `D:\vsProjects\preason-vite\config.json`
2. **修改内容**: 按照JSON格式修改
3. **保存文件**: Ctrl+S 保存
4. **刷新浏览器**: 立即看到效果

## 💡 提示

- **头像**: 可以使用在线图片URL，或留空使用默认占位符
- **图标**: 使用Emoji，如 ⚡、🚀、❤️、💻 等
- **颜色**: 使用十六进制颜色码，如 `#667eea`
- **ID**: 每个列表项的ID要唯一

## 🔧 高级配置

查看 `CONFIG_GUIDE.md` 获取详细配置指南

## 🎯 完成后的部署

```bash
# 构建生产版本
npm run build

# 构建后的文件在 dist/ 目录
# 可以部署到任何静态托管服务
```

---

**就这么简单！修改 `config.json` 就能完全自定义你的个人介绍页。**